[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
overflow-checks = true
incremental = true

[workspace]
members = [
    "client",
    "server",
    "zcdb",
    "zettacache",
    "zettaobject",
    "object_perf",
    "zoa",
]
